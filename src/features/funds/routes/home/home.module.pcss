.launcher {
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: row;
  gap: 1em;

  list-style: none;

  @media (--sm-n-below) {
    flex-direction: column;
  }
}

.launcher > li {
  flex: 0 1 calc(33%);
}

.welcome {
  color: var(--text2);
}

.profileName {
  color: var(--text1);
}

.launcherButton {
  display: block;
  overflow: hidden;

  border-radius: var(--radius-3);
  border: 4px solid var(--surface4);

  transition: all 0.27s var(--ease-3);

  &:hover,
  &:focus-within {
    border-color: var(--surfaceInvert);
    transform: scale(1.05);
    box-shadow: var(--shadow-4);
  }
}

/**
 * LauncherCard is like flow layout, text + icon, without a absolute background img
 * So we simulate it here, overwriting the card default style.
 */
.launcherButton {
  height: 300px;

  & > .launcherCard {
    height: 100%;
    justify-content: flex-end;

    & img {
      height: 50%;
    }
  }
}

/**
   * The emoji variant
   */
:global .launcherButton[data-variant='emoji'] {
  & .cardMedia {
    font-size: 200px;
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    /* display: block; */
    text-align: center;
  }

  & .cardLabel {
    font-size: var(--font-size-6);
    color: var(--textInvert);
    background-color: transparent;
    bottom: 0;
  }
}
